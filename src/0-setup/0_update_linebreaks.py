A script that we might need to update the linebeaks in all the scripts that get send to the cluster
As there are differences between windows and linux linebreaks, and touching those files with git somehow
changes the linebreak type (I think ...)